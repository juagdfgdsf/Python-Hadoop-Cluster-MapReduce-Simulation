# Python-Hadoop-Cluster-MapReduce-Simulation
Ground-up Python simulation of an Apache Hadoop cluster. Features HDFS block splitting, round-robin storage across 3 DataNodes, NameNode JSON tracking, and $2\times$ fault replication. Includes a custom MapReduce engine (word/category/sales metrics) and native K-Nearest Neighbors (KNN) AI model.
